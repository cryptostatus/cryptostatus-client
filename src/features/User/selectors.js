import { get } from 'utils'

export const accessHeadersSelector = get('user.accessHeaders')

// TODO: temporal implementation used in App.js for redirecting when user is not present
export const userSelector = get('user.accessHeaders')
